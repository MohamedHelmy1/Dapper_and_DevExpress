@model IEnumerable<Task1.DataModel.Products>

<h2>Products</h2>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>Name</th>
        <th>Price</th>
        @* <th>Actions</th> *@
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@item.Name</td>
            <td>@item.Price</td>
            @* <td> *@
            @*     @Html.ActionLink("Edit", "Edit", new { id = item.Id }) | *@
            @*     @Html.ActionLink("Delete", "Delete", new { id = item.Id }) *@
            @* </td> *@
        </tr>
    }
</table>
